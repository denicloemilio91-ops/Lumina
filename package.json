{
  "name": "lumiere-app",
  "version": "1.0.0",
  "description": "Lumière — Application de messages de réconfort et motivation générés par IA",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {},
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "deploy": "npx vercel --prod"
  }
}
